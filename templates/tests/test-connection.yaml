apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "praeco-elastalert-ui.fullname" . }}-test-connection"
  labels:
{{ include "praeco-elastalert-ui.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "praeco-elastalert-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never