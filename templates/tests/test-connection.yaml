apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "praeco_elastalert_ui.fullname" . }}-test-connection"
  labels:
{{ include "praeco_elastalert_ui.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "praeco_elastalert_ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never